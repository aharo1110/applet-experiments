{
  "name": "QR Code Generator",
  "description": "Generates QR codes from text.",
  "icons": [
    {
      "src": "icon-128x128.png"
    }
  ],
  "actions": {
    "generateQRCode": {
      "description": "Generates a QR code based on provided text.",
      "params_schema": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string",
            "description": "The text or URL to encode as a QR code."
          }
        },
        "required": ["text"]
      }
    }
  }
}
